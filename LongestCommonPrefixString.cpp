#include<bits/stdc++.h>
using namespace std;
string ans(vector<string> str){
    string r;
    for(int i = 0 ;i<str[0].size();i++){
        string c;
        c = str[i];
        for(int  j = 1;j<str.size();j++){
            if(i >= str.size() || c[i][j] != ){
                
            }
        }
    }
    return r;
}
int main(){
    vector<string> str = {"flower","flow","flight"} ;
    cout<<ans(str)<<endl;
}