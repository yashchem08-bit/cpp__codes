#include<bits/stdc++.h>
typedef std::vector<std::pair<std::string,int>> NEW_t;
int main(){
    NEW_t next; // NEW_t behaves as a datatype of {std::vector<std::pair<std::string,int>>} 
    // instead of witting whole vector<pair<string,int>> we would use NEW_t .....it is insane.....; 

}


#include<bits/stdc++.h>
using NEW_t = std::vector<std::pair<std::string,int>>; // it works exactly same as typpedef..
int main(){
   NEW_t next;
}